#!/bin/bash

test_hr="================= %s =================\n"
test_br="============================================\n"

app="psql cs421"
src_path_1="./PG-260835976/pgsql"
src_path_2="./PG-260835976/pgsql"

runTest () {
  printf "\n$test_hr" "$1 TESTS"
  ./test/bts/compare-results.bts "$app" $2 $3
  printf "$test_br"

  # # Pause by prompt
  #   read -p "Enter a to abort, anything else to continue: " input_data
  #   # Iff input is "a" then abort
  #   [ "$input_data" == "a" ] && break
}

runTest "SQL" $src_path_1 $src_path_2

echo

exit 0
