#!/bin/bash

test_hr="================= %s =================\n"
test_br="============================================\n"

app="psql cs421"
src_path="./PG-260835976/pgsql"

runTest () {
  printf "\n$test_hr" "$1 TESTS"
  ./test/bts/unittest.bts "$app" $2
  printf "$test_br"

  # # Pause by prompt
  #   read -p "Enter a to abort, anything else to continue: " input_data
  #   # Iff input is "a" then abort
  #   [ "$input_data" == "a" ] && break
}

runTest "SQL Queries" $src_path

echo

exit 0